# Runtime dependencies, pinned to the versions this project is tested against.
# Verified on Python 3.13 (see .github/workflows/ci.yml).
# Development tooling lives in requirements-dev.txt.

# Core numerics and modelling
numpy==2.5.2
pandas==2.3.3
scipy==1.18.0
scikit-learn==1.9.0

# Gradient-boosted models. Required, not optional: the benchmark suite
# trains all three families on every run.
xgboost==3.4.1
lightgbm==4.7.0

# Explainability
shap==0.52.0

# Typed configuration
pydantic==2.13.4
PyYAML==6.0.3

# Dataset access (UCI ML Repository dataset 350).
# Only needed for a live fetch; `python main.py --sample` runs without it.
ucimlrepo==0.0.7

# Experiment tracking. ExperimentTracker falls back to a local JSON run log
# when MLflow is absent or disabled, so this is a soft runtime requirement.
mlflow==3.15.1

# Plotting for the SHAP attribution chart
matplotlib==3.11.1

# Model persistence helper used by scikit-learn
joblib==1.5.3
